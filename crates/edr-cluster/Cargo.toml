[package]
name = "edr-cluster"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
edr-core = { path = "../edr-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3"
